class orderDetail {
  final String id;
  final String category;
  final String minimal_cost;
  final String maximal_cost;
  final String location;
  final String deadline;

  const orderDetail(this.id, this.category, this.minimal_cost,
      this.maximal_cost, this.location, this.deadline);
}
